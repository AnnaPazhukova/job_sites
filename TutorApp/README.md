# TutorSpace

Рабочее место репетитора математики: CRM для учеников и расписания + личная база заданий и методических заметок — в одном приложении с единым дизайном.

## Разделы

- **Мои ученики** — карточки учеников, баланс (переплата/задолженность), карточка ученика с занятиями и ДЗ
- **Группы** — объединение учеников для групповых занятий
- **Расписание** — календарь на месяц, разовые и регулярные занятия (ежедневно/еженедельно/ежемесячно)
- **Сообщения** — переписка с учениками
- **Проверка ДЗ** — задания, сроки, статусы (на проверке / проверено / просрочено)
- **База заданий** — личная коллекция задач с фильтрами по классу, предмету, теме, уровню, типу
- **Методика** — заметки о том, как вы объясняете темы, по классам
- **Статистика** — доходы, рост базы учеников, рабочее время, задолженности

## Разработка

```bash
npm install
npm run dev      # локальный запуск
npm run build    # сборка (tsc -b && vite build)
```

## Хранение данных

Данные хранятся за интерфейсом `DataAdapter` (`src/lib/storage.ts`), поэтому
бэкенд можно менять, не трогая компоненты и хук `useStore`.

- **Без настройки** (нет `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`) — данные
  живут в `localStorage` браузера, входа не требуется. Удобно для локальной
  разработки без своего проекта Supabase.
- **С Supabase** — при наличии обеих переменных окружения `AuthGate`
  (`src/AuthGate.tsx`) показывает экран входа/регистрации и после входа
  переключает хранилище на `SupabaseAdapter` (`src/lib/supabaseStorage.ts`):
  данные пишутся в таблицу `app_kv`, защищённую row-level security, так что
  каждый пользователь видит только свои данные и они доступны с любого
  устройства.

### Настройка Supabase

1. Создайте бесплатный проект на [supabase.com](https://supabase.com).
2. В SQL Editor выполните `supabase/schema.sql` (создаёт таблицу `app_kv` и
   политики RLS).
3. В Project Settings → API скопируйте **Project URL** и **anon public key**.
4. Для локальной разработки: скопируйте `.env.example` в `.env.local` и
   вставьте оба значения.
5. Для деплоя: добавьте оба значения как секреты репозитория GitHub
   (Settings → Secrets and variables → Actions) с именами
   `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` — workflow подставит их при
   сборке.

### Supabase Storage (файлы: картинки, PDF, Word)

Прикрепление файлов к домашним заданиям и урокам методики использует тот
же проект Supabase — отдельная настройка не нужна: `supabase/schema.sql`
уже создаёт публичный бакет `attachments` и политики доступа (загружать и
удалять можно только свои файлы, хранящиеся в папке с вашим user id).
Если бакет уже существовал до обновления схемы — просто выполните файл
`supabase/schema.sql` заново, лишнего он не создаст.

Без настроенного Supabase кнопка «Прикрепить файл» показывает подсказку и
не позволяет загружать — привязка урока методики к ДЗ (без файлов) при
этом продолжает работать в любом режиме.

## Google Calendar (опционально)

На странице «Расписание» можно подключить Google Calendar в режиме
только для чтения: события из основного календаря показываются в
месячной и недельной сетке (не создаются и не редактируются). Интеграция
полностью на стороне браузера, без бэкенда — токен доступа получается
через Google Identity Services и не сохраняется на сервере.

1. Зайдите на [console.cloud.google.com](https://console.cloud.google.com),
   создайте проект.
2. В «APIs & Services → Library» включите **Google Calendar API**.
3. В «APIs & Services → OAuth consent screen» настройте экран согласия
   (User Type: External), добавьте свой аккаунт в Test users — этого
   достаточно для личного использования, без прохождения верификации Google.
4. В «APIs & Services → Credentials» создайте **OAuth client ID** типа
   **Web application**. В Authorized JavaScript origins добавьте:
   - `https://annapazhukova.github.io`
   - `http://localhost:5173` (для локальной разработки)
5. Скопируйте Client ID (вида `xxxxx.apps.googleusercontent.com`).
6. Локально: добавьте `VITE_GOOGLE_CLIENT_ID` в `.env.local`.
7. Для деплоя: добавьте `VITE_GOOGLE_CLIENT_ID` как секрет репозитория
   GitHub (Settings → Secrets and variables → Actions).

Без этой переменной кнопка подключения Google Calendar просто не
показывается — остальной функционал не затрагивается.

## Личный кабинет ученика

Работает только при подключённом Supabase (см. выше) — своего отдельного
проекта не нужно, используется тот же `supabase/schema.sql`, в который
добавлены таблицы `student_invites`, `student_accounts` и несколько
`SECURITY DEFINER` функций (`student_get_data`, `student_send_message`,
`student_mark_homework_done`, `claim_student_invite`).

Как это устроено:

1. На странице ученика (карточка ученика → «Пригласить в кабинет») репетитор
   создаёт одноразовый код-приглашение и получает ссылку вида
   `https://.../?invite=XXXX` — её нужно отправить ученику.
2. Ученик переходит по ссылке, создаёт свой аккаунт (email + пароль) — это
   отдельный аккаунт Supabase Auth, не связанный с аккаунтом репетитора.
   Приглашение одноразовое: после успешной регистрации код становится
   недействителен.
3. После входа приложение определяет роль автоматически: если для этого
   аккаунта есть привязка «ученик → репетитор», показывается личный кабинет
   ученика вместо CRM репетитора.
4. В кабинете ученик видит **только свои** данные — расписание, домашние
   задания (включая прикреплённый урок из методики и файлы) и переписку.
   Все эти данные читаются не напрямую из таблицы `app_kv` (она по-прежнему
   доступна только репетитору по RLS), а через функции на стороне базы,
   которые сами фильтруют массив по `studentId`, прежде чем что-либо вернуть
   — так что ученик физически не может получить данные других учеников.
5. Ученик может отмечать домашние задания как выполненные и писать
   сообщения репетитору — оба действия тоже идут через отдельные функции с
   проверкой, что ученик пишет/меняет только свои же записи.

Если Supabase не настроен, кнопка «Пригласить в кабинет» не показывается —
остальной функционал не затрагивается.
